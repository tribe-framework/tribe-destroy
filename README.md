# Tribe Destroy (Uninstall and Delete)

### quick un-install
```
sudo bash -c "$(wget --no-cache --no-cookie https://raw.githubusercontent.com/tribe-framework/tribe-destroy/master/uninstall.sh -O -)"
```
